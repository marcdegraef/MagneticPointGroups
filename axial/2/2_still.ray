#include "../preface_still.ray"


name point_group
list
	object axis_two 

/* 2 */

	object pparrow rotate 0 1 0 ( theta) translate ( px) ( py) ( pz) 
	object pparrow rotate 0 1 0 (-theta) translate (mpx) (mpy) ( pz) 

	object refaxis
end


#include "../tail_still.ray"
