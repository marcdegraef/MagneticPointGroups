#include "../preface_still.ray"

name point_group
list
	object anti_axis_two 
	object mirrorplane
        object anti_sympoint

/* 2'/m */

	object pparrow rotate 0 1 0 ( theta) translate ( px) ( py) ( pz) 
	object pmarrow scale 1 1 -1 rotate 0 1 0 (-theta) translate (mpx) (mpy) ( pz) 
	object mmarrow scale 1 1 -1 rotate 0 1 0 ( theta) translate (mpx) (mpy) (mpz) 
	object mparrow rotate 0 1 0 (-theta) translate ( px) ( py) (mpz) 

	object refaxis
end

#include "../tail_still.ray"

